using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EmptyCanScript : MonoBehaviour {

    public bool flag;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //GameObject.Find("BucketStartButton").GetComponent<BuketGameStart>().SetGameStartFlag(flag);
    }
}
